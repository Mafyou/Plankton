# Render Blueprint for Plankton API
# Service ID (existing service): srv-d81cvn3bc2fs738t62eg
# Public URL: https://plankton-376v.onrender.com

services:
  - type: web
    name: plankton
    env: docker
    dockerfilePath: ./Dockerfile
